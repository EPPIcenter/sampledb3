# Local development (Bun loads this automatically; gitignored as `.env.local`)
# Copy: cp .env.local.example .env.local

# SQLite file for the API. Default when unset: ./sampledb_dev.sqlite (empty dev DB).
# Uncomment to use your production snapshot or another DB file:
# DATABASE_PATH=./sampledb_database.sqlite
