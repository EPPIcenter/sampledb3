# Root workspace test config. Use `bun run test` to run API (bun) + web (vitest). Use `bun run test:e2e` for E2E (Playwright).
# Limit discovery to API src so we don't run: api/dist (missing fixtures), e2e (Playwright), or web (needs Vitest/jsdom).
[test]
root = "packages/api/src"
